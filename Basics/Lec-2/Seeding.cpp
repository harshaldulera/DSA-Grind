#include <bits/stdc++.h>
using namespace std;

#define PI 3.14
#define long long ll

void seeding(int n) {
    for(int i = 0; i < n; i++) {
        for(int j = n - i; j > 0; j--) {
            cout << "*" << " ";
        }
        cout << endl;
    }
}

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    

    return 0;
}